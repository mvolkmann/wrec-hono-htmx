# wrec-hono-htmx

This is a web app that demonstrates using htmx to send requests
to Hono endpoints that return wrec-based custom elements.

To run this:

1. `bun install`
1. `bun run bundle`
1. `bun run dev`
1. Browse localhost:3000.
1. Click the "Greet" button.
